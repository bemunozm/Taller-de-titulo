# Logs (solo runtime en la raiz, NO src/components/logs que es codigo)
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# CodeGraph (indice local; codegraph.json se versiona)
.codegraph/

# Env local (puede contener secrets como VITE_KIOSK_HUB_SECRET) — usar .env.example como plantilla
.env
.env.local
.env.*.local
